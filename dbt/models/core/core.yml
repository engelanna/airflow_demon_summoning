version: 2

models:
  - name: core__claims
    config:
      alias: claims
    description: Claims, but with [date_created] coalesced from [staging.TREATMENT]
    tests:
      - dbt_utils.equal_rowcount:
          compare_model: ref("staging__claims")
          severity: error
  # - name: cor__invoices
  # - name: cor__invoice_items
  # - name: cor__treatments

