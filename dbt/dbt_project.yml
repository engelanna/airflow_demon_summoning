name: "carepay_engelanna"
version: "1.0.0"
config-version: 2

profile: "carepay_engelanna"

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets:
    - "target"
    - "dbt_modules"

models:
    sde_dbt_tutorial:
        core:
            materialized: table
